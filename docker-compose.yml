version: "3.8"
services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - MONGO_URI=mongodb+srv://benedictkpaduwamongo:Benedict101@webtracker.ehzynvm.mongodb.net/?appName=webtracker
    depends_on:
      - mongo
  mongo:
    image: mongo:6
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
